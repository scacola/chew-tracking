# Chew Coach 랜딩 페이지

옵션 G "Chew & Calm Coach" 마케팅 랜딩 페이지.

## 로컬 실행

```bash
cd landing
npm install
npm run dev
```

## 빌드

```bash
npm run build
```

## 배포

`main` 브랜치에 `landing/**` 변경이 push되면 GitHub Actions가 자동으로 GitHub Pages에 배포합니다.

배포 URL: https://scacola.github.io/chew-tracking/
